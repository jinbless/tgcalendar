version: "3.8"

services:
  telegram-bot:
    build: .
    env_file:
      - .env
    volumes:
      - ./data/tokens:/app/data/tokens
    restart: unless-stopped
    environment:
      - TZ=Asia/Seoul
