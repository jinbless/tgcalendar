version: "3.8"

services:
  telegram-bot:
    build: .
    env_file:
      - .env
    volumes:
      - ./data/tokens:/app/data/tokens
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - TZ=Asia/Seoul
